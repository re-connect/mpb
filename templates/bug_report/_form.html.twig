{{ form_start(form) }}
    {{ form_row(form.title) }}
    {{ form_row(form.content) }}
<div class="w-50">
    {{ form_row(form.category) }}
</div>

<button class="btn btn-primary">{{ button_label }}</button>
{{ form_end(form) }}
