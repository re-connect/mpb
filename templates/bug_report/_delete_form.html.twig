<form method="post" class="d-flex" action="{{ path('bug_report_delete', {'id': bug_report.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bug_report.id) }}">
    <button class="btn btn-danger ms-auto"><i class="fas fa-trash"></i></button>
</form>
