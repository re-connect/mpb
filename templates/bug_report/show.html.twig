{% extends 'base.html.twig' %}

{% block title %}BugReport{% endblock %}

{% block body %}
    <h1>BugReport</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bug_report.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ bug_report.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ bug_report.content }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ bug_report.createdAt ? bug_report.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ bug_report.updatedAt ? bug_report.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ bug_report.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bug_report_index') }}">back to list</a>

    <a href="{{ path('bug_report_edit', {'id': bug_report.id}) }}">edit</a>

    {{ include('bug_report/_delete_form.html.twig') }}
{% endblock %}
