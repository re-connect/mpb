{{ form_start(form) }}
    <div class="d-flex">
        {{ form_widget(form.status, {'attr': {'class': 'w-75'}}) }}
        <button type="submit" class="btn btn-secondary mx-2">{{ 'edit_status'|trans }}</button>
        {{ form_errors(form) }}
    </div>
{{ form_end(form) }}
