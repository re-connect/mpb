<button
        class="btn btn-light"
        {{ stimulus_controller('browser-navigation') }}
        {{ stimulus_action('browser-navigation', 'back') }}
>
    <i class="fa fa-chevron-left me-3"></i>Retour
</button>
